services:
  lastboard:
    build: .
    container_name: lastboard
    ports:
      - "${LASTBOARD_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
    restart: unless-stopped
