This directory is used to embed the frontend build assets into the Go binary.
In production, the build process replaces this with the actual content of `web/dist`.
This readme ensures the directory is not empty for local development.
